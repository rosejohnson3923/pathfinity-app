/**
 * Fix Root Width Utility - DISABLED
 * This function is now disabled in favor of a pure CSS solution
 * See src/styles/root-layout.css for the new approach
 */

export function fixRootWidth(): void {
  // DISABLED - Using pure CSS solution instead
  console.log('fixRootWidth: Disabled - using CSS-only solution');
  return;
}